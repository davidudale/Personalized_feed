services:
  - type: web
    name: personalized-news-aggregator
    env: python
    runtime: python-3.11
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: gunicorn personalized_news_aggregator.app:app
    plan: free
